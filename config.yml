title: History lesson
description: This is a hhistory course
template:
  repo: history-lesson-template
  name: history-lesson
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
